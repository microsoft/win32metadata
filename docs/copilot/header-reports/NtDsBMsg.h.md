# NtDsBMsg.h  - **Owning partition:** ActiveDirectory (001) - **Patch:** None - **Classification:** blocked  Targeted logical comparison is blocked because Windows Defender denies startup of the repository-built `win32metadata-tools.exe` with OS error 225. The SDK ingestion path and patch apply/reverse checks pass, but compiling or applying a patch is not sufficient to claim a metadata match.  **Next action:** run ActiveDirectory x64 comparison in approved CI, resolve any logical delta, then validate x86 and arm64. 
