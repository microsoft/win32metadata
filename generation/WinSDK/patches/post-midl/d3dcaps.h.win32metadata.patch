diff --git a/generation/WinSDK/RecompiledIdlHeaders/um/d3dcaps.h b/generation/WinSDK/RecompiledIdlHeaders/um/d3dcaps.h
index 15e419ffb2..e13fc173f0 100644
--- a/generation/WinSDK/RecompiledIdlHeaders/um/d3dcaps.h
+++ b/generation/WinSDK/RecompiledIdlHeaders/um/d3dcaps.h
@@ -1,3 +1,4 @@
+#include <win32metadata_annotations.h>
 /*==========================================================================;
  *
  *  Copyright (C) Microsoft Corporation.  All Rights Reserved. 
@@ -53,11 +54,19 @@ typedef struct _D3DLIGHTINGCAPS {
 #define D3DLIGHTCAPS_SPOT               0x00000002L /* Spot lights supported */
 #define D3DLIGHTCAPS_DIRECTIONAL        0x00000004L /* Directional lights supported */
 #if(DIRECT3D_VERSION < 0x700)
+#endif
+#if (defined(WIN32METADATA) && !defined(D3DLIGHTCAPS_PARALLELPOINT)) || ((((DIRECT3D_VERSION < 0x700))))
 #define D3DLIGHTCAPS_PARALLELPOINT      0x00000008L /* Parallel point lights supported */
 #endif
+#if(DIRECT3D_VERSION < 0x700)
+#endif
 #if(DIRECT3D_VERSION < 0x500)
+#endif
+#if (defined(WIN32METADATA) && !defined(D3DLIGHTCAPS_GLSPOT)) || ((((DIRECT3D_VERSION < 0x500))))
 #define D3DLIGHTCAPS_GLSPOT             0x00000010L /* GL syle spot lights supported */
 #endif
+#if(DIRECT3D_VERSION < 0x500)
+#endif
 
 /* Description of capabilities for each primitive type */
 
@@ -438,10 +447,10 @@ typedef struct _D3DDeviceDesc7 {
 #define D3DDEVICEDESCSIZE (sizeof(D3DDEVICEDESC))
 #define D3DDEVICEDESC7SIZE (sizeof(D3DDEVICEDESC7))
 
-typedef HRESULT (CALLBACK * LPD3DENUMDEVICESCALLBACK)(GUID FAR *lpGuid, LPSTR lpDeviceDescription, LPSTR lpDeviceName, LPD3DDEVICEDESC, LPD3DDEVICEDESC, LPVOID);
+typedef HRESULT (CALLBACK * LPD3DENUMDEVICESCALLBACK)(_WIN32META_ANNOTATION_("win32metadata:in") _WIN32META_ANNOTATION_("win32metadata:out") GUID FAR *lpGuid, LPSTR lpDeviceDescription, LPSTR lpDeviceName, _WIN32META_ANNOTATION_("win32metadata:in") _WIN32META_ANNOTATION_("win32metadata:out") LPD3DDEVICEDESC, _WIN32META_ANNOTATION_("win32metadata:in") _WIN32META_ANNOTATION_("win32metadata:out") LPD3DDEVICEDESC, _WIN32META_ANNOTATION_("win32metadata:in") _WIN32META_ANNOTATION_("win32metadata:out") LPVOID);
 
 #if(DIRECT3D_VERSION >= 0x0700)
-typedef HRESULT (CALLBACK * LPD3DENUMDEVICESCALLBACK7)(LPSTR lpDeviceDescription, LPSTR lpDeviceName, LPD3DDEVICEDESC7, LPVOID);
+typedef HRESULT (CALLBACK * LPD3DENUMDEVICESCALLBACK7)(LPSTR lpDeviceDescription, LPSTR lpDeviceName, _WIN32META_ANNOTATION_("win32metadata:in") _WIN32META_ANNOTATION_("win32metadata:out") LPD3DDEVICEDESC7, _WIN32META_ANNOTATION_("win32metadata:in") _WIN32META_ANNOTATION_("win32metadata:out") LPVOID);
 #endif /* DIRECT3D_VERSION >= 0x0700 */
 
 /* D3DDEVICEDESC dwFlags indicating valid fields */
