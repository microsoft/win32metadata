diff --git a/generation/WinSDK/RecompiledIdlHeaders/um/printoem.h b/generation/WinSDK/RecompiledIdlHeaders/um/printoem.h
index c094c73308..6508983b00 100644
--- a/generation/WinSDK/RecompiledIdlHeaders/um/printoem.h
+++ b/generation/WinSDK/RecompiledIdlHeaders/um/printoem.h
@@ -1,3 +1,4 @@
+#include <win32metadata_annotations.h>
 /*++
 
 Copyright (c) 1996-2004 Microsoft Corporation
@@ -123,7 +124,7 @@ OEMDriverDMS(
 #define OEMDM_CONVERT  3
 #define OEMDM_MERGE    4
 
-typedef struct _OEMDMPARAM {
+typedef struct _WIN32META_ANNOTATION_("win32metadata:struct_size_field=cbSize")  _OEMDMPARAM {
 
     DWORD    cbSize;        // size of OEM_DEVMODEPARAM
     PVOID    pdriverobj;    // reference to driver data structure
@@ -171,7 +172,7 @@ OEMDevMode(
 //
 
 typedef BOOL (APIENTRY *PFN_DrvGetDriverSetting)(
-        PVOID                           pdriverobj,
+        _WIN32META_ANNOTATION_("win32metadata:in") _WIN32META_ANNOTATION_("win32metadata:out") PVOID                           pdriverobj,
         PCSTR                           Feature,
         _Out_writes_bytes_opt_(cbSize) PVOID  pOutput,
         DWORD                           cbSize,
@@ -197,8 +198,8 @@ typedef BOOL (APIENTRY *PFN_DrvUpgradeRegistrySetting)(
 //
 
 typedef BOOL (APIENTRY *PFN_DrvUpdateUISetting)(
-        PVOID    pdriverobj,
-        PVOID    pOptItem,
+        _WIN32META_ANNOTATION_("win32metadata:in") _WIN32META_ANNOTATION_("win32metadata:out") PVOID    pdriverobj,
+        _WIN32META_ANNOTATION_("win32metadata:in") _WIN32META_ANNOTATION_("win32metadata:out") PVOID    pOptItem,
         DWORD    dwPreviousSelection,
         DWORD    dwMode
         );
@@ -374,12 +375,36 @@ typedef BOOL (APIENTRY *PFN_DrvUnidriverTextOut)(
 //  0x8000
 //
 //
+#endif
+#if defined(WIN32METADATA) || ((defined(KERNEL_MODE)))
 #define MV_UPDATE       0x0001
+#endif
+#ifdef KERNEL_MODE
+#endif
+#if defined(WIN32METADATA) || ((defined(KERNEL_MODE)))
 #define MV_RELATIVE     0x0002
+#endif
+#ifdef KERNEL_MODE
+#endif
+#if defined(WIN32METADATA) || ((defined(KERNEL_MODE)))
 #define MV_GRAPHICS     0x0004
+#endif
+#ifdef KERNEL_MODE
+#endif
+#if defined(WIN32METADATA) || ((defined(KERNEL_MODE)))
 #define MV_PHYSICAL     0x0008
+#endif
+#ifdef KERNEL_MODE
+#endif
+#if defined(WIN32METADATA) || ((defined(KERNEL_MODE)))
 #define MV_SENDXMOVECMD     0x0010
+#endif
+#ifdef KERNEL_MODE
+#endif
+#if defined(WIN32METADATA) || ((defined(KERNEL_MODE)))
 #define MV_SENDYMOVECMD     0x0020
+#endif
+#ifdef KERNEL_MODE
 
 typedef struct _DRVPROCS {
 
@@ -571,10 +596,26 @@ OEMTTYGetInfo(
     _Out_ DWORD                 *pcbcNeeded
     );
 
+#endif
+#if defined(WIN32METADATA) || ((defined(KERNEL_MODE)))
 #define OEMTTY_INFO_MARGINS     1
+#endif
+#ifdef KERNEL_MODE
+#endif
+#if defined(WIN32METADATA) || ((defined(KERNEL_MODE)))
 #define OEMTTY_INFO_CODEPAGE    2
+#endif
+#ifdef KERNEL_MODE
+#endif
+#if defined(WIN32METADATA) || ((defined(KERNEL_MODE)))
 #define OEMTTY_INFO_NUM_UFMS    3
+#endif
+#ifdef KERNEL_MODE
+#endif
+#if defined(WIN32METADATA) || ((defined(KERNEL_MODE)))
 #define OEMTTY_INFO_UFM_IDS     4
+#endif
+#ifdef KERNEL_MODE
 
 //
 // UNIDRV font callback
@@ -593,16 +634,52 @@ typedef struct _UNIFONTOBJ {
 // UNIFONTOBJ.dwFlags
 //
 
+#endif
+#if defined(WIN32METADATA) || ((defined(KERNEL_MODE)))
 #define UFOFLAG_TTFONT               0x00000001
+#endif
+#ifdef KERNEL_MODE
+#endif
+#if defined(WIN32METADATA) || ((defined(KERNEL_MODE)))
 #define UFOFLAG_TTDOWNLOAD_BITMAP    0x00000002
+#endif
+#ifdef KERNEL_MODE
+#endif
+#if defined(WIN32METADATA) || ((defined(KERNEL_MODE)))
 #define UFOFLAG_TTDOWNLOAD_TTOUTLINE 0x00000004
+#endif
+#ifdef KERNEL_MODE
 
 #if (NTDDI_VERSION >= NTDDI_WINXP)
 
+#endif
+#endif
+#if defined(WIN32METADATA) || ((defined(KERNEL_MODE)) && ((NTDDI_VERSION >= NTDDI_WINXP)))
 #define UFOFLAG_TTOUTLINE_BOLD_SIM   0x00000008
+#endif
+#ifdef KERNEL_MODE
+#if (NTDDI_VERSION >= NTDDI_WINXP)
+#endif
+#endif
+#if defined(WIN32METADATA) || ((defined(KERNEL_MODE)) && ((NTDDI_VERSION >= NTDDI_WINXP)))
 #define UFOFLAG_TTOUTLINE_ITALIC_SIM 0x00000010
+#endif
+#ifdef KERNEL_MODE
+#if (NTDDI_VERSION >= NTDDI_WINXP)
+#endif
+#endif
+#if defined(WIN32METADATA) || ((defined(KERNEL_MODE)) && ((NTDDI_VERSION >= NTDDI_WINXP)))
 #define UFOFLAG_TTOUTLINE_VERTICAL   0x00000020
+#endif
+#ifdef KERNEL_MODE
+#if (NTDDI_VERSION >= NTDDI_WINXP)
+#endif
+#endif
+#if defined(WIN32METADATA) || ((defined(KERNEL_MODE)) && ((NTDDI_VERSION >= NTDDI_WINXP)))
 #define UFOFLAG_TTSUBSTITUTED        0x00000040
+#endif
+#ifdef KERNEL_MODE
+#if (NTDDI_VERSION >= NTDDI_WINXP)
 
 #endif // (NTDDI_VERSION >= NTDDI_WINXP)
 
@@ -610,12 +687,36 @@ typedef struct _UNIFONTOBJ {
 // UNIFONTOBJ callback ID
 //
 
+#endif
+#if defined(WIN32METADATA) || ((defined(KERNEL_MODE)))
 #define UFO_GETINFO_FONTOBJ     1
+#endif
+#ifdef KERNEL_MODE
+#endif
+#if defined(WIN32METADATA) || ((defined(KERNEL_MODE)))
 #define UFO_GETINFO_GLYPHSTRING 2
+#endif
+#ifdef KERNEL_MODE
+#endif
+#if defined(WIN32METADATA) || ((defined(KERNEL_MODE)))
 #define UFO_GETINFO_GLYPHBITMAP 3
+#endif
+#ifdef KERNEL_MODE
+#endif
+#if defined(WIN32METADATA) || ((defined(KERNEL_MODE)))
 #define UFO_GETINFO_GLYPHWIDTH  4
+#endif
+#ifdef KERNEL_MODE
+#endif
+#if defined(WIN32METADATA) || ((defined(KERNEL_MODE)))
 #define UFO_GETINFO_MEMORY      5
+#endif
+#ifdef KERNEL_MODE
+#endif
+#if defined(WIN32METADATA) || ((defined(KERNEL_MODE)))
 #define UFO_GETINFO_STDVARIABLE 6
+#endif
+#ifdef KERNEL_MODE
 
 //
 // UFO_GETINFO_FONTOBJ callback structure
@@ -688,21 +789,81 @@ typedef struct _GETINFO_STDVAR {
 } GETINFO_STDVAR, *PGETINFO_STDVAR;
 
 
+#endif
+#if defined(WIN32METADATA) || ((defined(KERNEL_MODE)))
 #define FNT_INFO_PRINTDIRINCCDEGREES  0 // PrintDirInCCDegrees
+#endif
+#ifdef KERNEL_MODE
+#endif
+#if defined(WIN32METADATA) || ((defined(KERNEL_MODE)))
 #define FNT_INFO_GRAYPERCENTAGE       1 // GrayPercentage
+#endif
+#ifdef KERNEL_MODE
+#endif
+#if defined(WIN32METADATA) || ((defined(KERNEL_MODE)))
 #define FNT_INFO_NEXTFONTID           2 // NextfontID
+#endif
+#ifdef KERNEL_MODE
+#endif
+#if defined(WIN32METADATA) || ((defined(KERNEL_MODE)))
 #define FNT_INFO_NEXTGLYPH            3 // NextGlyph
+#endif
+#ifdef KERNEL_MODE
+#endif
+#if defined(WIN32METADATA) || ((defined(KERNEL_MODE)))
 #define FNT_INFO_FONTHEIGHT           4 // FontHeight
+#endif
+#ifdef KERNEL_MODE
+#endif
+#if defined(WIN32METADATA) || ((defined(KERNEL_MODE)))
 #define FNT_INFO_FONTWIDTH            5 // FontWidth
+#endif
+#ifdef KERNEL_MODE
+#endif
+#if defined(WIN32METADATA) || ((defined(KERNEL_MODE)))
 #define FNT_INFO_FONTBOLD             6 // FontBold
+#endif
+#ifdef KERNEL_MODE
+#endif
+#if defined(WIN32METADATA) || ((defined(KERNEL_MODE)))
 #define FNT_INFO_FONTITALIC           7 // FontItalic
+#endif
+#ifdef KERNEL_MODE
+#endif
+#if defined(WIN32METADATA) || ((defined(KERNEL_MODE)))
 #define FNT_INFO_FONTUNDERLINE        8 // FontUnderline
+#endif
+#ifdef KERNEL_MODE
+#endif
+#if defined(WIN32METADATA) || ((defined(KERNEL_MODE)))
 #define FNT_INFO_FONTSTRIKETHRU       9 // FontStrikeThru
+#endif
+#ifdef KERNEL_MODE
+#endif
+#if defined(WIN32METADATA) || ((defined(KERNEL_MODE)))
 #define FNT_INFO_CURRENTFONTID       10 // Current
+#endif
+#ifdef KERNEL_MODE
+#endif
+#if defined(WIN32METADATA) || ((defined(KERNEL_MODE)))
 #define FNT_INFO_TEXTYRES            11 // TextYRes
+#endif
+#ifdef KERNEL_MODE
+#endif
+#if defined(WIN32METADATA) || ((defined(KERNEL_MODE)))
 #define FNT_INFO_TEXTXRES            12 // TextXRes
+#endif
+#ifdef KERNEL_MODE
+#endif
+#if defined(WIN32METADATA) || ((defined(KERNEL_MODE)))
 #define FNT_INFO_FONTMAXWIDTH        13 // FontMaxWidth
+#endif
+#ifdef KERNEL_MODE
+#endif
+#if defined(WIN32METADATA) || ((defined(KERNEL_MODE)))
 #define FNT_INFO_MAX                 14
+#endif
+#ifdef KERNEL_MODE
 
 //
 // OEMDownloadFontheader - UNIDRV only
@@ -739,10 +900,26 @@ OEMTTDownloadMethod(
     PUNIFONTOBJ pUFObj
     );
 
+#endif
+#if defined(WIN32METADATA) || ((defined(KERNEL_MODE)))
 #define TTDOWNLOAD_DONTCARE  0
+#endif
+#ifdef KERNEL_MODE
+#endif
+#if defined(WIN32METADATA) || ((defined(KERNEL_MODE)))
 #define TTDOWNLOAD_GRAPHICS  1
+#endif
+#ifdef KERNEL_MODE
+#endif
+#if defined(WIN32METADATA) || ((defined(KERNEL_MODE)))
 #define TTDOWNLOAD_BITMAP    2
+#endif
+#ifdef KERNEL_MODE
+#endif
+#if defined(WIN32METADATA) || ((defined(KERNEL_MODE)))
 #define TTDOWNLOAD_TTOUTLINE 3
+#endif
+#ifdef KERNEL_MODE
 
 //
 // OEMOutputCharStr - UNIDRV only
@@ -758,10 +935,26 @@ OEMOutputCharStr(
     _In_reads_(dwCount) PVOID  pGlyph
     );
 
+#endif
+#if defined(WIN32METADATA) || ((defined(KERNEL_MODE)))
 #define TYPE_UNICODE      1
+#endif
+#ifdef KERNEL_MODE
+#endif
+#if defined(WIN32METADATA) || ((defined(KERNEL_MODE)))
 #define TYPE_TRANSDATA    2
+#endif
+#ifdef KERNEL_MODE
+#endif
+#if defined(WIN32METADATA) || ((defined(KERNEL_MODE)))
 #define TYPE_GLYPHHANDLE  3
+#endif
+#ifdef KERNEL_MODE
+#endif
+#if defined(WIN32METADATA) || ((defined(KERNEL_MODE)))
 #define TYPE_GLYPHID      4
+#endif
+#ifdef KERNEL_MODE
 
 //
 // OEMSendFontCmd - UNIDRV only
@@ -1241,7 +1434,13 @@ OEMTransparentBlt(
 //
 // for the IPrintOemPS2::GetPDEVAdjustment call
 //
+#endif
+#endif
+#if defined(WIN32METADATA) || ((defined(KERNEL_MODE)) && ((NTDDI_VERSION >= NTDDI_WINXP)))
 #define PDEV_ADJUST_PAPER_MARGIN_TYPE 1
+#endif
+#ifdef KERNEL_MODE
+#if (NTDDI_VERSION >= NTDDI_WINXP)
 
 typedef struct _PDEV_ADJUST_PAPER_MARGIN {
 
@@ -1249,7 +1448,13 @@ typedef struct _PDEV_ADJUST_PAPER_MARGIN {
 
 } PDEV_ADJUST_PAPER_MARGIN;
 
+#endif
+#endif
+#if defined(WIN32METADATA) || ((defined(KERNEL_MODE)) && ((NTDDI_VERSION >= NTDDI_WINXP)))
 #define PDEV_HOSTFONT_ENABLED_TYPE 2
+#endif
+#ifdef KERNEL_MODE
+#if (NTDDI_VERSION >= NTDDI_WINXP)
 
 typedef struct _PDEV_HOSTFONT_ENABLED {
 
@@ -1257,7 +1462,13 @@ typedef struct _PDEV_HOSTFONT_ENABLED {
 
 } PDEV_HOSTFONT_ENABLED;
 
+#endif
+#endif
+#if defined(WIN32METADATA) || ((defined(KERNEL_MODE)) && ((NTDDI_VERSION >= NTDDI_WINXP)))
 #define PDEV_USE_TRUE_COLOR_TYPE 3
+#endif
+#ifdef KERNEL_MODE
+#if (NTDDI_VERSION >= NTDDI_WINXP)
 
 typedef struct _PDEV_USE_TRUE_COLOR {
 
@@ -1270,9 +1481,27 @@ typedef struct _PDEV_USE_TRUE_COLOR {
 
 #if (NTDDI_VERSION >= NTDDI_VISTA)
 
+#endif
+#endif
+#if defined(WIN32METADATA) || ((defined(KERNEL_MODE)) && ((NTDDI_VERSION >= NTDDI_VISTA)))
 #define PDEV_ADJUST_GRAPHICS_RESOLUTION_TYPE          (0x1 << 2) //0x4
+#endif
+#ifdef KERNEL_MODE
+#if (NTDDI_VERSION >= NTDDI_VISTA)
+#endif
+#endif
+#if defined(WIN32METADATA) || ((defined(KERNEL_MODE)) && ((NTDDI_VERSION >= NTDDI_VISTA)))
 #define PDEV_ADJUST_IMAGEABLE_ORIGIN_AREA_TYPE        (0x1 << 3) //0x8
+#endif
+#ifdef KERNEL_MODE
+#if (NTDDI_VERSION >= NTDDI_VISTA)
+#endif
+#endif
+#if defined(WIN32METADATA) || ((defined(KERNEL_MODE)) && ((NTDDI_VERSION >= NTDDI_VISTA)))
 #define PDEV_ADJUST_PHYSICAL_PAPER_SIZE_TYPE          (0x1 << 4) //0x10
+#endif
+#ifdef KERNEL_MODE
+#if (NTDDI_VERSION >= NTDDI_VISTA)
 
 typedef struct _PDEV_ADJUST_IMAGEABLE_ORIGIN_AREA {
 
@@ -1347,7 +1576,7 @@ typedef struct _OEMUIPROCS {
 } OEMUIPROCS, *POEMUIPROCS;
 
 
-typedef struct _OEMUIOBJ {
+typedef struct _WIN32META_ANNOTATION_("win32metadata:struct_size_field=cbSize")  _OEMUIOBJ {
 
     DWORD           cbSize;             // size of this structure
     POEMUIPROCS     pOemUIProcs;        // pointer to user mode helper function table
@@ -1359,9 +1588,9 @@ typedef struct _OEMUIOBJ {
 //
 
 typedef struct _OEMCUIPPARAM *POEMCUIPPARAM;
-typedef LONG (APIENTRY *OEMCUIPCALLBACK)(PCPSUICBPARAM, POEMCUIPPARAM);
+typedef LONG (APIENTRY *OEMCUIPCALLBACK)(_WIN32META_ANNOTATION_("win32metadata:in") _WIN32META_ANNOTATION_("win32metadata:out") PCPSUICBPARAM, _WIN32META_ANNOTATION_("win32metadata:in") _WIN32META_ANNOTATION_("win32metadata:out") POEMCUIPPARAM);
 
-typedef struct _OEMCUIPPARAM {
+typedef struct _WIN32META_ANNOTATION_("win32metadata:struct_size_field=cbSize")  _OEMCUIPPARAM {
 
     DWORD           cbSize;         // size of this structure
     POEMUIOBJ       poemuiobj;      // reference to driver data structure
@@ -1419,7 +1648,7 @@ OEMDevicePropertySheets(
 //
 // pPSUIInfo->lParamInit is a pointer to _OEMUIPSPARAM structure defined below.
 //
-typedef struct _OEMUIPSPARAM {
+typedef struct _WIN32META_ANNOTATION_("win32metadata:struct_size_field=cbSize")  _OEMUIPSPARAM {
 
     DWORD           cbSize;         // size of this structure
     POEMUIOBJ       poemuiobj;      // reference to driver data structure
