diff --git a/generation/WinSDK/RecompiledIdlHeaders/um/tnef.h b/generation/WinSDK/RecompiledIdlHeaders/um/tnef.h
index d7e2254f77..1e9745c25a 100644
--- a/generation/WinSDK/RecompiledIdlHeaders/um/tnef.h
+++ b/generation/WinSDK/RecompiledIdlHeaders/um/tnef.h
@@ -18,6 +18,7 @@
 #pragma once
 #endif
 #include <winapifamily.h>
+#include <win32metadata_annotations.h>
 
 #pragma region Desktop Family
 #if WINAPI_FAMILY_PARTITION(WINAPI_PARTITION_DESKTOP)
@@ -100,16 +101,16 @@ DECLARE_MAPI_INTERFACE_PTR(ITnef, LPITNEF);
 	MAPIMETHOD(AddProps)												\
 		(THIS_	ULONG						ulFlags,					\
 				ULONG						ulElemID,					\
-				LPVOID						lpvData,					\
-				LPSPropTagArray				lpPropList) IPURE;			\
-	MAPIMETHOD(ExtractProps)											\
+				 LPVOID						lpvData,					\
+				 LPSPropTagArray				lpPropList) IPURE;			\
+	 MAPIMETHOD(ExtractProps)											\
 		(THIS_	ULONG						ulFlags,					\
 				LPSPropTagArray				lpPropList,					\
-				LPSTnefProblemArray FAR *	lpProblems) IPURE;			\
-	MAPIMETHOD(Finish)													\
+				 LPSTnefProblemArray FAR *	lpProblems) IPURE;			\
+	 MAPIMETHOD(Finish)													\
 		(THIS_	ULONG						ulFlags,					\
 				WORD FAR *					lpKey,						\
-				LPSTnefProblemArray FAR *	lpProblems) IPURE;			\
+				 LPSTnefProblemArray FAR *	lpProblems) IPURE;			\
 	MAPIMETHOD(OpenTaggedBody)											\
 		(THIS_	LPMESSAGE					lpMessage,					\
 				ULONG						ulFlags,					\
@@ -118,7 +119,7 @@ DECLARE_MAPI_INTERFACE_PTR(ITnef, LPITNEF);
 		(THIS_	ULONG						ulFlags,					\
 				ULONG						ulElemID,					\
 				ULONG						cValues,					\
-				LPSPropValue				lpProps) IPURE;				\
+				 LPSPropValue				lpProps) IPURE;				\
 	MAPIMETHOD(EncodeRecips)											\
 		(THIS_	ULONG						ulFlags,					\
 				LPMAPITABLE					lpRecipientTable) IPURE;	\
@@ -128,7 +129,7 @@ DECLARE_MAPI_INTERFACE_PTR(ITnef, LPITNEF);
 				LPSPropTagArray				lpCustomPropList,			\
 				LPSPropValue				lpCustomProps,				\
 				LPSPropTagArray				lpPropList,					\
-				LPSTnefProblemArray FAR *	lpProblems) IPURE;			\
+				 LPSTnefProblemArray FAR *	lpProblems) IPURE;			\
 
 #undef		 INTERFACE
 #define		 INTERFACE  ITnef
