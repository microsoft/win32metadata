diff --git a/generation/WinSDK/RecompiledIdlHeaders/um/ScrnSave.h b/generation/WinSDK/RecompiledIdlHeaders/um/ScrnSave.h
index f98fb18c3b..d929aaaf4d 100644
--- a/generation/WinSDK/RecompiledIdlHeaders/um/ScrnSave.h
+++ b/generation/WinSDK/RecompiledIdlHeaders/um/ScrnSave.h
@@ -27,6 +27,10 @@
 *                                                                             *
 \*****************************************************************************/
 
+#if defined(WIN32METADATA)
+#include <win32metadata_annotations.h>
+#endif
+
 #ifndef _INC_SCRNSAVE
 #define _INC_SCRNSAVE
 
@@ -84,6 +88,7 @@ extern "C" {            /* Assume C declarations for C++ */
 LRESULT WINAPI ScreenSaverProcW (HWND hWnd, UINT message, WPARAM wParam, LPARAM lParam);
 #   define  ScreenSaverProc ScreenSaverProcW
 #else
+_Windows_SupportedOS_WindowsXP_
 LRESULT WINAPI ScreenSaverProc (HWND hWnd, UINT message, WPARAM wParam, LPARAM lParam);
 #endif
 
@@ -118,6 +123,7 @@ LRESULT WINAPI ScreenSaverProc (HWND hWnd, UINT message, WPARAM wParam, LPARAM l
  *                  This message is ignored, however is the password option
  *                  is enabled.
  */
+_Windows_SupportedOS_WindowsXP_
 LRESULT WINAPI DefScreenSaverProc (HWND hWnd, UINT msg, WPARAM wParam, LPARAM lParam);
 
 /* A function is also needed for configuring the screen saver.  The function
@@ -126,6 +132,7 @@ LRESULT WINAPI DefScreenSaverProc (HWND hWnd, UINT msg, WPARAM wParam, LPARAM lP
  * more, the template used for the dialog must be called
  * ScreenSaverConfigure to allow the main function to access it...
  */
+_Windows_SupportedOS_WindowsXP_
 BOOL WINAPI ScreenSaverConfigureDialog (HWND hDlg, UINT message, WPARAM wParam, LPARAM lParam);
 
 /* To allow the programmer the ability to register child control windows, this
@@ -133,6 +140,7 @@ BOOL WINAPI ScreenSaverConfigureDialog (HWND hDlg, UINT message, WPARAM wParam,
  * registering that is required should be done here, or return TRUE if none
  * is needed...
  */
+_Windows_SupportedOS_WindowsXP_
 BOOL WINAPI RegisterDialogClasses (HANDLE hInst);
 
 /* The following functions are called by DefScreenSaverProc and must
