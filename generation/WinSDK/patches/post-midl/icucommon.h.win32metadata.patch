diff --git a/generation/WinSDK/RecompiledIdlHeaders/um/icucommon.h b/generation/WinSDK/RecompiledIdlHeaders/um/icucommon.h
index d9f0b11cd2..1133d54065 100644
--- a/generation/WinSDK/RecompiledIdlHeaders/um/icucommon.h
+++ b/generation/WinSDK/RecompiledIdlHeaders/um/icucommon.h
@@ -1,3 +1,4 @@
+#include <win32metadata_annotations.h>
 // icucommon.h is autogenerated and merged from the ICU header files.
 // Code unused or not supported in the Windows ICU SDK has been removed.
 
@@ -138,6 +139,10 @@
 *   created by: Markus W. Scherer
 */
 
+#if (defined(WIN32METADATA) && !defined(UCONFIG_FORMAT_FASTPATHS_49)) || (((!defined(__UCONFIG_H__))) && ((!defined(UCONFIG_FORMAT_FASTPATHS_49))))
+#   define UCONFIG_FORMAT_FASTPATHS_49 1
+#endif
+
 #ifndef __UCONFIG_H__
 #define __UCONFIG_H__
 
@@ -298,8 +303,13 @@
 #ifdef U_HAVE_LIB_SUFFIX
     /* Use the predefined value. */
 #elif defined(U_LIB_SUFFIX_C_NAME)
+#endif
+#if (defined(WIN32METADATA) && !defined(U_HAVE_LIB_SUFFIX)) || ((!(defined(U_HAVE_LIB_SUFFIX)) && (defined(U_LIB_SUFFIX_C_NAME))))
 #   define U_HAVE_LIB_SUFFIX 1
 #endif
+#ifdef U_HAVE_LIB_SUFFIX
+#elif defined(U_LIB_SUFFIX_C_NAME)
+#endif
 
 /**
  * \def U_LIB_SUFFIX_C_NAME_STRING
@@ -550,7 +560,6 @@
  * @internal
  */
 #ifndef UCONFIG_FORMAT_FASTPATHS_49
-#   define UCONFIG_FORMAT_FASTPATHS_49 1
 #endif
 
 /**
@@ -617,7 +626,24 @@
 ******************************************************************************
 */
 
+#if (defined(WIN32METADATA) && !defined(U_IOSTREAM_SOURCE)) || (((!defined(_PLATFORM_H))) && ((!defined(U_IOSTREAM_SOURCE))))
+#define U_IOSTREAM_SOURCE 199711
+#endif
+
+#if (defined(WIN32METADATA) && !defined(U_PLATFORM_HAS_WINUWP_API)) || (((!defined(_PLATFORM_H))) && (!(defined(U_PLATFORM_HAS_WINUWP_API))))
+#   define U_PLATFORM_HAS_WINUWP_API 0
+#endif
+
+#if defined(WIN32METADATA) && defined(__clang__)
+// Keep the original platform initialization gate while exposing the legacy capability macro.
+#pragma push_macro("_WIN32META_ICUCOMMON_PLATFORM_BODY")
+#undef _WIN32META_ICUCOMMON_PLATFORM_BODY
 #ifndef _PLATFORM_H
+#define _WIN32META_ICUCOMMON_PLATFORM_BODY
+#endif
+#endif
+#if !defined(_PLATFORM_H) || (defined(WIN32METADATA) && defined(__clang__))
+#if !defined(WIN32METADATA) || !defined(__clang__) || defined(_WIN32META_ICUCOMMON_PLATFORM_BODY)
 #define _PLATFORM_H
 
 
@@ -839,7 +865,6 @@
 #ifdef U_PLATFORM_HAS_WINUWP_API
  /* Use the predefined value. */
 #else
-#   define U_PLATFORM_HAS_WINUWP_API 0
 #endif
 
 /**
@@ -949,7 +974,6 @@
  * @internal
  */
 #ifndef U_IOSTREAM_SOURCE
-#define U_IOSTREAM_SOURCE 199711
 #endif
 
 /**
@@ -1085,6 +1109,8 @@
  * C++11 feature, necessary for move constructor & move assignment.
  * @internal
  */
+#endif
+
 #ifdef U_HAVE_RVALUE_REFERENCES
     /* Use the predefined value. */
 #elif U_CPLUSPLUS_VERSION >= 11 || __has_feature(cxx_rvalue_references) \
@@ -1095,6 +1121,8 @@
 #   define U_HAVE_RVALUE_REFERENCES 0
 #endif
 
+#if !defined(WIN32METADATA) || !defined(__clang__) || defined(_WIN32META_ICUCOMMON_PLATFORM_BODY)
+
 /**
  * \def U_NOEXCEPT
  * "noexcept" if supported, otherwise empty.
@@ -1435,6 +1463,10 @@
 
 /* @} */
 
+#endif
+#endif
+#if defined(WIN32METADATA) && defined(__clang__)
+#pragma pop_macro("_WIN32META_ICUCOMMON_PLATFORM_BODY")
 #endif
 
 // ptypes.h
@@ -12759,6 +12791,10 @@ u_setMemoryFunctions(const void *context, UMemAllocFn *a, UMemReallocFn *r, UMem
 ******************************************************************************
 */
 
+#if (defined(WIN32METADATA) && !defined(U_UNICODE_VERSION)) || (((!defined(UCHAR_H))))
+#define U_UNICODE_VERSION "8.0"
+#endif
+
 #ifndef UCHAR_H
 #define UCHAR_H
 
@@ -12777,7 +12813,6 @@ U_CDECL_BEGIN
  * @see u_getUnicodeVersion
  * @stable ICU 2.0
  */
-#define U_UNICODE_VERSION "8.0"
 
 /**
  * \file
@@ -24143,9 +24178,9 @@ u_strlen(const UChar *s);
  */
 typedef int32_t U_CALLCONV
 UStringCaseMapper(const UCaseMap *csm,
-                  UChar *dest, int32_t destCapacity,
+                  _WIN32META_ANNOTATION_("win32metadata:in") _WIN32META_ANNOTATION_("win32metadata:out") UChar *dest, int32_t destCapacity,
                   const UChar *src, int32_t srcLength,
-                  UErrorCode *pErrorCode);
+                  _WIN32META_ANNOTATION_("win32metadata:in") _WIN32META_ANNOTATION_("win32metadata:out") UErrorCode *pErrorCode);
 
 #endif
 
